fastapi==0.111.0
uvicorn[standard]==0.29.0
slowapi==0.1.9
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.1

# Base de données
psycopg2-binary==2.9.9
sqlalchemy==2.0.30

# NB : la stratégie « APIs tierces » remplace les modèles ML locaux.
# Les modules appellent Google Safe Browsing, Sightengine et Claude (httpx),
# avec repli heuristique. Pour réintroduire des modèles self-hosted plus tard
# (torch, sentence-transformers, clip…), voir backend/train_deepfake.py.
